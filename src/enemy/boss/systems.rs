//! Boss-specific systems. Currently empty — all Boss behavior is
//! expressed via universal capability components (`Regeneration`,
//! `Armor`, `SplitsOnDeath`) plus the screen-shake observer registered
//! in `mod.rs`. This file exists so future Boss-only systems have an
//! obvious home.
